package com.interfaces;

public interface Circle {
	float pie = 3.14F;	// Every variables created in an interface is public, static and final

	void calculateArea();	// The interface can have only contains public and abstract method
}
